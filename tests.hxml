--library ansi
--library hashlink
--library hlpicohttp
--library tink_core
--library tink_macro
--library tink_priority
--library tink_streams
--library tink_syntaxhub
--library tink_testrunner
--library tink_unittest
--class-path tests
--macro include("xing", true)
--main TestRunner
--dce full
--define no-deprecation-warnings
--resource res/base.html@baseHtml
--each

--next
--hl out/tests.c